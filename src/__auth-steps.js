/**
AUTH SETUP
1. Create firebase project
2. Enable web and sign in method
3. Install firebase
4. Get firebase config in your firebase.config.js file and export it as "export default app" 
 */

/**
1. Create UserContext (Auth Context): userContext ----> component name, userContext provides AuthContext
2. Create AuthContext and set AuthContext.Provider
3. Make sure you set children, provide value, and export AuthContext to be used inside useContext hook
4. Get form data
5. getAuth in the UserContext
 */